apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: scaleway-secret-manager
spec:
  provider:
    scaleway:
      region: fr-par
      projectId: dddbc56e-210a-4e64-a46e-40d5b088e3fa
      accessKey:
        secretRef:
          name: scaleway-api-credentials
          namespace: external-secrets
          key: access-key
      secretKey:
        secretRef:
          name: scaleway-api-credentials
          namespace: external-secrets
          key: secret-key
