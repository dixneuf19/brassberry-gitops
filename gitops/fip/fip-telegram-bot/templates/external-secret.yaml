apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fip-telegram-bot
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: scaleway-secret-manager
    kind: ClusterSecretStore
  target:
    name: fip-telegram-bot
  dataFrom:
    - extract:
        key: name:fip-telegram-bot
        version: latest_enabled
