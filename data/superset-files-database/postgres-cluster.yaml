apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: superset-files-database
  namespace: data
spec:
  instances: 1
  enablePDB: false

  storage:
    size: 1Gi
    
  resources:
    requests:
      cpu: 1
      memory: 256Mi

  monitoring:
    enablePodMonitor: true
